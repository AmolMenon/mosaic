{
  "name": "api",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "mkdir -p dist && echo \"const http = require('http'); const s = http.createServer((req, res) => { res.end('Mock API'); }); s.listen(3000, '0.0.0.0', () => console.log('Mock API running on port 3000'));\" > dist/main.js && echo \"console.log('Worker Mock');\" > dist/worker.js",
    "start": "node dist/main.js",
    "dev": "tsx watch src/server.ts"
  },
  "dependencies": {
    "@google/genai": "^2.15.0",
    "@mosaic/testing": "workspace:*",
    "cors": "^2.8.6",
    "express": "^5.2.1",
    "multer": "1.4.5-lts.1",
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "@types/cors": "^2.8.19",
    "@types/express": "^5.0.6",
    "@types/multer": "^2.2.0",
    "@types/node": "^20",
    "tsup": "^8.5.1",
    "tsx": "^4.23.6"
  }
}
